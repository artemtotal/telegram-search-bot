# Мінімум, щоб прогнати тести. Основний requirements.txt тягне ще fastembed,
# onnxruntime і telethon — вони потрібні боту в роботі, але не тестам, і
# важать стільки, що ставити їх на кожен прогін немає сенсу. Тести, яким вони
# справді потрібні, пропускають себе самі.
python-telegram-bot==13.15
sqlalchemy==1.4.46
requests>=2.28.0
numpy>=1.24.0,<2.0
